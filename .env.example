# Claude Session Browser — Configuration
# Copy to .env and adjust if needed. All values have sensible defaults.

# Path to your Claude Code sessions directory.
# Default: ~/.claude/projects (standard Claude Code location)
# CLAUDE_SESSIONS_PATH=~/.claude/projects

# Browser UI port
# PORT=5173

# Backend API port
# API_PORT=3001
